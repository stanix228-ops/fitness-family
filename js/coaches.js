const COACHES_DATA = [
  {
    "id": 1,
    "name": "Ольга Малышева",
    "category": "VIP",
    "halls": [
      "VM 1.0",
      "VM 4.0"
    ],
    "instagram": "ola_zhuzhu",
    "image": "images/coaches/coach_1.png"
  },
  {
    "id": 2,
    "name": "Игорь Горбунов",
    "category": "MASTER",
    "halls": [
      "VM 1.0"
    ],
    "instagram": "igorfitproof",
    "image": "images/coaches/coach_2.png"
  },
  {
    "id": 3,
    "name": "Мария Чмир",
    "category": "VIP",
    "halls": [
      "VM 2.0"
    ],
    "instagram": "mariella_ch",
    "image": "images/coaches/coach_3.png"
  },
  {
    "id": 4,
    "name": "Женис Сабитов",
    "category": "VIP",
    "halls": [
      "VM 1.0",
      "VM 2.0",
      "VM 4.0"
    ],
    "instagram": "jeniss.fit",
    "image": "images/coaches/coach_4.png"
  },
  {
    "id": 5,
    "name": "Дарья Ивченко",
    "category": "BASIC",
    "halls": [
      "VM 1.0",
      "VM 2.0",
      "VM 4.0"
    ],
    "instagram": "ivchenko_fit.coach",
    "image": "images/coaches/coach_5.png"
  },
  {
    "id": 6,
    "name": "Илья Никонов",
    "category": "VIP",
    "halls": [
      "VM 2.0",
      "VM 4.0"
    ],
    "instagram": "blessed.coach_",
    "image": "images/coaches/coach_6.png"
  },
  {
    "id": 7,
    "name": "Константин Ефимов",
    "category": "BASIC",
    "halls": [
      "VM 1.0",
      "VM 2.0",
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "efimov_ko",
    "image": "images/coaches/coach_7.png"
  },
  {
    "id": 8,
    "name": "Самрат Мухаметкали",
    "category": "BASIC",
    "halls": [
      "VM 2.0",
      "VM 3.0"
    ],
    "instagram": "sam.fitbro",
    "image": "images/coaches/coach_8.png"
  },
  {
    "id": 9,
    "name": "Темирлан Сагитов",
    "category": "MASTER",
    "halls": [
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "sag1tov",
    "image": "images/coaches/coach_9.png"
  },
  {
    "id": 10,
    "name": "Евгений Соболев",
    "category": "VIP",
    "halls": [
      "VM 2.0",
      "VM 4.0"
    ],
    "instagram": "sobolev.gymkz",
    "image": "images/coaches/coach_10.png"
  },
  {
    "id": 11,
    "name": "Алтынай Мукашева",
    "category": "BASIC",
    "halls": [
      "VM 1.0",
      "VM 2.0",
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "fittusya",
    "image": "images/coaches/coach_11.png"
  },
  {
    "id": 12,
    "name": "Римма Потеряева",
    "category": "BASIC",
    "halls": [
      "VM 4.0"
    ],
    "instagram": "rimmeoww",
    "image": "images/coaches/coach_12.png"
  },
  {
    "id": 13,
    "name": "Асель Садуова",
    "category": "MASTER",
    "halls": [
      "VM 2.0",
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "sadu_assel",
    "image": "images/coaches/coach_13.png"
  },
  {
    "id": 14,
    "name": "Виктория Сабо",
    "category": "BASIC",
    "halls": [
      "VM 1.0",
      "VM 3.0"
    ],
    "instagram": "sabo.fitgirl_",
    "image": "images/coaches/coach_14.png"
  },
  {
    "id": 15,
    "name": "Михаил Верховных",
    "category": "VIP",
    "halls": [
      "VM 1.0",
      "VM 2.0",
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "luna_gymkz",
    "image": "images/coaches/coach_15.png"
  },
  {
    "id": 16,
    "name": "Никита Ковальски",
    "category": "VIP",
    "halls": [
      "VM 2.0",
      "VM 4.0"
    ],
    "instagram": "coach_kovalskii",
    "image": "images/coaches/coach_16.png"
  },
  {
    "id": 17,
    "name": "Анастасия Ободзинская",
    "category": "MASTER",
    "halls": [
      "VM 2.0",
      "VM 3.0"
    ],
    "instagram": "nasstasiss.obodz",
    "image": "images/coaches/coach_17.png"
  },
  {
    "id": 18,
    "name": "Марина Илюхина",
    "category": "BASIC",
    "halls": [
      "VM 3.0"
    ],
    "instagram": "1ilyukhina",
    "image": "images/coaches/coach_18.png"
  },
  {
    "id": 19,
    "name": "Кристина Иноземцева",
    "category": "MASTER",
    "halls": [
      "VM 2.0",
      "VM 4.0"
    ],
    "instagram": "kris_fit_ogon13",
    "image": "images/coaches/coach_19.png"
  },
  {
    "id": 20,
    "name": "Александр Касимов",
    "category": "MASTER",
    "halls": [
      "VM 1.0",
      "VM 2.0",
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "neffor.coach",
    "image": "images/coaches/coach_20.png"
  },
  {
    "id": 21,
    "name": "Марина Лопатина",
    "category": "MASTER",
    "halls": [
      "VM 2.0",
      "VM 3.0"
    ],
    "instagram": "marina_fit_trainer",
    "image": "images/coaches/coach_21.png"
  },
  {
    "id": 22,
    "name": "Алмагуль Мухамедиева",
    "category": "MASTER",
    "halls": [
      "VM 1.0",
      "VM 4.0"
    ],
    "instagram": "_almagul_k_m",
    "image": "images/coaches/coach_22.png"
  },
  {
    "id": 23,
    "name": "Алена Куприянова",
    "category": "BASIC",
    "halls": [
      "VM 2.0",
      "VM 4.0"
    ],
    "instagram": "petro_trener_alena",
    "image": "images/coaches/coach_23.png"
  },
  {
    "id": 24,
    "name": "Милана Емельянова",
    "category": "BASIC",
    "halls": [
      "VM 1.0",
      "VM 4.0"
    ],
    "instagram": "1i_am_milana1",
    "image": "images/coaches/coach_24.png"
  },
  {
    "id": 25,
    "name": "Ольга Герасимова",
    "category": "MASTER",
    "halls": [
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "fit_trener_olga",
    "image": "images/coaches/coach_25.png"
  },
  {
    "id": 26,
    "name": "Анастасия Калеганова",
    "category": "BASIC",
    "halls": [
      "VM 4.0"
    ],
    "instagram": "sillveeeee",
    "image": "images/coaches/coach_26.png"
  },
  {
    "id": 27,
    "name": "Егор Остертак",
    "category": "BASIC",
    "halls": [
      "VM 2.0",
      "VM 3.0"
    ],
    "instagram": "coach_tesrtix",
    "image": "images/coaches/coach_27.png"
  },
  {
    "id": 28,
    "name": "Александр Ветер",
    "category": "BASIC",
    "halls": [
      "VM 2.0",
      "VM 3.0"
    ],
    "instagram": "_v3ter.coach",
    "image": "images/coaches/coach_28.png"
  },
  {
    "id": 29,
    "name": "Кристина Урицких",
    "category": "BASIC",
    "halls": [
      "VM 1.0",
      "VM 4.0"
    ],
    "instagram": "lfkmassagekris",
    "image": "images/coaches/coach_29.png"
  },
  {
    "id": 30,
    "name": "Андрей Тимощук",
    "category": "BASIC",
    "halls": [
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "a.tymosh",
    "image": "images/coaches/coach_30.png"
  },
  {
    "id": 31,
    "name": "Вероника Соколова",
    "category": "BASIC",
    "halls": [
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "1skllv",
    "image": "images/coaches/coach_31.png"
  },
  {
    "id": 32,
    "name": "Ксения Сидоренко",
    "category": "BASIC",
    "halls": [
      "VM 4.0"
    ],
    "instagram": "kseniaa.sidorenko",
    "image": "images/coaches/coach_32.png"
  },
  {
    "id": 33,
    "name": "Данила Сафронов",
    "category": "BASIC",
    "halls": [
      "VM 1.0",
      "VM 2.0",
      "VM 3.0",
      "VM 4.0"
    ],
    "instagram": "_safronovdan",
    "image": "images/coaches/coach_33.png"
  },
  {
    "id": 34,
    "name": "Татьяна Даниленко",
    "category": "BASIC",
    "halls": [
      "VM 1.0"
    ],
    "instagram": "coach_petro_15",
    "image": "images/coaches/coach_34.png"
  }
];

function getCategoryBadgeClass(category) {
  if (category === 'VIP') return 'coach-badge-vip';
  if (category === 'MASTER') return 'coach-badge-master';
  return 'coach-badge-basic';
}

function formatHalls(halls) {
  if (halls.length >= 4) {
    return 'Все филиалы';
  }
  return halls.join(' • ');
}

function createCoachCardHTML(coach, isModal) {
  isModal = !!isModal;
  const igUrl = 'https://www.instagram.com/' + coach.instagram + '/';

  return `
    <div class="coach-card group ${isModal ? 'w-full' : ''}">
      <!-- Image Container -->
      <div class="relative overflow-hidden bg-black aspect-[9/16] sm:h-[400px]">
        <img 
          src="${coach.image}" 
          alt="${coach.name}" 
          class="coach-img" 
          loading="lazy"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-[#0E0E0E] via-transparent to-transparent pointer-events-none"></div>
      </div>

      <!-- Content Footer -->
      <div class="p-4 flex-1 flex flex-col justify-between bg-[#0E0E0E] border-t border-[#1C1C1C] text-center">
        <div class="mb-3">
          <h4 class="font-oswald font-bold text-lg text-white group-hover:text-neutral-200 transition-colors tracking-wide leading-tight text-center">
            ${coach.name}
          </h4>
        </div>

        <a 
          href="${igUrl}" 
          target="_blank" 
          rel="noopener noreferrer" 
          class="w-full py-2.5 px-3 bg-white hover:bg-neutral-200 text-black font-oswald font-bold text-xs tracking-wider uppercase rounded flex items-center justify-center gap-2 transition-all duration-200 shadow-md hover:shadow-lg active:scale-[0.98]"
        >
          <i class="fa-brands fa-instagram text-sm"></i>
          ЗАПИСАТЬСЯ
        </a>
      </div>
    </div>
  `;
}

let carouselAnimFrame = null;
let isCarouselPaused = false;
let carouselResumeTimeout = null;

function initCoachesCarousel() {
  const track = document.getElementById('coaches-track');
  const prevBtn = document.getElementById('coaches-prev-btn');
  const nextBtn = document.getElementById('coaches-next-btn');
  const openModalBtn = document.getElementById('open-coaches-modal-btn');
  const openModalBtn2 = document.getElementById('open-coaches-modal-btn-2');

  if (!track) return;

  // Duplicate cards for seamless infinite scroll
  const doubleCoaches = COACHES_DATA.concat(COACHES_DATA);
  track.innerHTML = doubleCoaches.map(coach => createCoachCardHTML(coach, false)).join('');

  // Get single card step (card width + gap)
  function getCardStep() {
    const firstCard = track.querySelector('.coach-card');
    return firstCard ? firstCard.offsetWidth + 20 : 320;
  }

  function pauseAutoscroll(durationMs = 4000) {
    isCarouselPaused = true;
    if (carouselResumeTimeout) clearTimeout(carouselResumeTimeout);
    if (durationMs > 0) {
      carouselResumeTimeout = setTimeout(() => {
        isCarouselPaused = false;
      }, durationMs);
    }
  }

  // Arrow Button: Prev (⟨)
  if (prevBtn) {
    prevBtn.onclick = function(e) {
      e.preventDefault();
      pauseAutoscroll(4000);
      const step = getCardStep();
      if (track.scrollLeft <= 10) {
        const halfWidth = track.scrollWidth / 2;
        track.scrollLeft = halfWidth;
      }
      track.scrollBy({ left: -step, behavior: 'smooth' });
    };
  }

  // Arrow Button: Next (⟩)
  if (nextBtn) {
    nextBtn.onclick = function(e) {
      e.preventDefault();
      pauseAutoscroll(4000);
      const step = getCardStep();
      const halfWidth = track.scrollWidth / 2;
      if (track.scrollLeft >= halfWidth) {
        track.scrollLeft = track.scrollLeft - halfWidth;
      }
      track.scrollBy({ left: step, behavior: 'smooth' });
    };
  }

  // Smooth Auto-Scrolling Engine with subpixel precision
  let scrollAccumulator = 0;
  const speed = 0.75; // pixels per frame

  function stepAutoscroll() {
    if (!isCarouselPaused) {
      scrollAccumulator += speed;
      if (scrollAccumulator >= 1) {
        const px = Math.floor(scrollAccumulator);
        scrollAccumulator -= px;
        track.scrollLeft += px;

        const halfWidth = track.scrollWidth / 2;
        if (track.scrollLeft >= halfWidth) {
          track.scrollLeft -= halfWidth;
        }
      }
    }
    carouselAnimFrame = requestAnimationFrame(stepAutoscroll);
  }

  // Start continuous smooth loop
  if (carouselAnimFrame) cancelAnimationFrame(carouselAnimFrame);
  carouselAnimFrame = requestAnimationFrame(stepAutoscroll);

  // Pause on Mouse Enter / Resume on Mouse Leave
  track.addEventListener('mouseenter', () => {
    isCarouselPaused = true;
  });

  track.addEventListener('mouseleave', () => {
    if (!isPointerDown) isCarouselPaused = false;
  });

  // Touch & Mouse Drag Engine for smooth mobile & desktop dragging
  let isPointerDown = false;
  let startX = 0;
  let scrollLeftStart = 0;
  let hasDragged = false;

  // Touch handlers for mobile
  track.addEventListener('touchstart', (e) => {
    isCarouselPaused = true;
    isPointerDown = true;
    hasDragged = false;
    startX = e.touches[0].pageX - track.offsetLeft;
    scrollLeftStart = track.scrollLeft;
  }, { passive: true });

  track.addEventListener('touchmove', (e) => {
    if (!isPointerDown) return;
    const x = e.touches[0].pageX - track.offsetLeft;
    const walk = (x - startX);
    if (Math.abs(walk) > 4) {
      hasDragged = true;
    }
    track.scrollLeft = scrollLeftStart - walk;

    const halfWidth = track.scrollWidth / 2;
    if (track.scrollLeft >= halfWidth) {
      track.scrollLeft -= halfWidth;
      scrollLeftStart -= halfWidth;
    } else if (track.scrollLeft <= 0) {
      track.scrollLeft += halfWidth;
      scrollLeftStart += halfWidth;
    }
  }, { passive: true });

  track.addEventListener('touchend', () => {
    isPointerDown = false;
    pauseAutoscroll(3000);
  });

  track.addEventListener('touchcancel', () => {
    isPointerDown = false;
    pauseAutoscroll(2000);
  });

  // Mouse Drag handlers
  track.addEventListener('mousedown', (e) => {
    if (e.button !== 0) return;
    isCarouselPaused = true;
    isPointerDown = true;
    hasDragged = false;
    startX = e.pageX - track.offsetLeft;
    scrollLeftStart = track.scrollLeft;
  });

  window.addEventListener('mousemove', (e) => {
    if (!isPointerDown) return;
    const x = e.pageX - track.offsetLeft;
    const walk = (x - startX);
    if (Math.abs(walk) > 4) {
      hasDragged = true;
    }
    track.scrollLeft = scrollLeftStart - walk;

    const halfWidth = track.scrollWidth / 2;
    if (track.scrollLeft >= halfWidth) {
      track.scrollLeft -= halfWidth;
      scrollLeftStart -= halfWidth;
    } else if (track.scrollLeft <= 0) {
      track.scrollLeft += halfWidth;
      scrollLeftStart += halfWidth;
    }
  });

  window.addEventListener('mouseup', () => {
    if (isPointerDown) {
      isPointerDown = false;
      pauseAutoscroll(3000);
    }
  });

  // Prevent link click when user is dragging
  track.addEventListener('click', (e) => {
    if (hasDragged) {
      e.preventDefault();
      e.stopPropagation();
    }
  }, true);

  // Mouse wheel horizontal scroll support
  track.addEventListener('wheel', (e) => {
    if (Math.abs(e.deltaY) > Math.abs(e.deltaX)) {
      e.preventDefault();
      pauseAutoscroll(3000);
      track.scrollLeft += e.deltaY;
    }
  }, { passive: false });

  // Modal open handlers
  if (openModalBtn) openModalBtn.onclick = openAllCoachesModal;
  if (openModalBtn2) openModalBtn2.onclick = openAllCoachesModal;
}

let activeHallFilter = 'ALL';
let activeCategoryFilter = 'ALL';
let currentSearchQuery = '';

function openAllCoachesModal() {
  const modal = document.getElementById('coaches-modal');
  if (!modal) return;

  activeHallFilter = 'ALL';
  activeCategoryFilter = 'ALL';
  currentSearchQuery = '';

  const searchInput = document.getElementById('coaches-modal-search');
  if (searchInput) searchInput.value = '';

  resetCoachesFiltersUI();
  renderModalCoaches();
  modal.classList.remove('hidden');
  document.body.style.overflow = 'hidden';
}

function closeAllCoachesModal() {
  const modal = document.getElementById('coaches-modal');
  if (!modal) return;
  modal.classList.add('hidden');
  document.body.style.overflow = '';
}

function filterCoaches() {
  return COACHES_DATA.filter(coach => {
    let matchesHall = true;
    if (activeHallFilter !== 'ALL') {
      matchesHall = coach.halls.includes(activeHallFilter) || (coach.halls.includes('VM 1.0') && coach.halls.includes('VM 2.0') && coach.halls.includes('VM 3.0') && coach.halls.includes('VM 4.0'));
    }

    let matchesCategory = true;
    if (activeCategoryFilter !== 'ALL') {
      matchesCategory = coach.category === activeCategoryFilter;
    }

    let matchesSearch = true;
    if (currentSearchQuery.trim()) {
      const q = currentSearchQuery.toLowerCase().trim();
      matchesSearch = coach.name.toLowerCase().includes(q) || coach.instagram.toLowerCase().includes(q);
    }

    return matchesHall && matchesCategory && matchesSearch;
  });
}

function renderModalCoaches() {
  const grid = document.getElementById('all-coaches-grid');
  const countBadge = document.getElementById('coaches-count-badge');
  if (!grid) return;

  const filtered = filterCoaches();

  if (countBadge) {
    countBadge.textContent = filtered.length + ' из ' + COACHES_DATA.length;
  }

  if (filtered.length === 0) {
    grid.innerHTML = `
      <div class="col-span-full py-16 text-center">
        <i class="fa-solid fa-user-slash text-4xl text-neutral-600 mb-4 block"></i>
        <h3 class="font-oswald text-xl text-white uppercase tracking-wider mb-2">Тренеры не найдены</h3>
        <p class="text-xs text-neutral-400 font-montserrat">Попробуйте изменить параметры поиска или сбросить фильтры</p>
        <button id="reset-filters-btn" class="mt-4 px-4 py-2 bg-neutral-800 hover:bg-neutral-700 text-xs font-oswald uppercase tracking-wider rounded text-white transition-colors">
          Сбросить фильтры
        </button>
      </div>
    `;
    const resetBtn = document.getElementById('reset-filters-btn');
    if (resetBtn) resetBtn.addEventListener('click', resetCoachesFilters);
    return;
  }

  grid.innerHTML = filtered.map(coach => createCoachCardHTML(coach, true)).join('');
}

function resetCoachesFiltersUI() {
  document.querySelectorAll('.hall-filter-btn').forEach(btn => {
    if (btn.dataset.hall === 'ALL') {
      btn.className = 'hall-filter-btn px-3 py-1.5 rounded text-xs font-oswald uppercase tracking-wider bg-white text-black font-bold transition-all';
    } else {
      btn.className = 'hall-filter-btn px-3 py-1.5 rounded text-xs font-oswald uppercase tracking-wider bg-[#141414] hover:bg-[#222222] text-neutral-400 transition-all';
    }
  });

  document.querySelectorAll('.cat-filter-btn').forEach(btn => {
    if (btn.dataset.cat === 'ALL') {
      btn.className = 'cat-filter-btn px-3 py-1.5 rounded text-xs font-oswald uppercase tracking-wider bg-white text-black font-bold transition-all';
    } else {
      btn.className = 'cat-filter-btn px-3 py-1.5 rounded text-xs font-oswald uppercase tracking-wider bg-[#141414] hover:bg-[#222222] text-neutral-400 transition-all';
    }
  });
}

function resetCoachesFilters() {
  activeHallFilter = 'ALL';
  activeCategoryFilter = 'ALL';
  currentSearchQuery = '';

  const searchInput = document.getElementById('coaches-modal-search');
  if (searchInput) searchInput.value = '';

  resetCoachesFiltersUI();
  renderModalCoaches();
}

function initCoachesModalEvents() {
  const modal = document.getElementById('coaches-modal');
  const closeBtn = document.getElementById('close-coaches-modal-btn');
  const searchInput = document.getElementById('coaches-modal-search');

  if (closeBtn) {
    closeBtn.addEventListener('click', closeAllCoachesModal);
  }

  if (modal) {
    modal.addEventListener('click', (e) => {
      if (e.target === modal) {
        closeAllCoachesModal();
      }
    });
  }

  window.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      closeAllCoachesModal();
    }
  });

  if (searchInput) {
    searchInput.addEventListener('input', (e) => {
      currentSearchQuery = e.target.value;
      renderModalCoaches();
    });
  }

  document.querySelectorAll('.hall-filter-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      activeHallFilter = btn.dataset.hall;
      document.querySelectorAll('.hall-filter-btn').forEach(b => {
        b.className = 'hall-filter-btn px-3 py-1.5 rounded text-xs font-oswald uppercase tracking-wider bg-[#141414] hover:bg-[#222222] text-neutral-400 transition-all';
      });
      btn.className = 'hall-filter-btn px-3 py-1.5 rounded text-xs font-oswald uppercase tracking-wider bg-white text-black font-bold transition-all';
      renderModalCoaches();
    });
  });

  document.querySelectorAll('.cat-filter-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      activeCategoryFilter = btn.dataset.cat;
      document.querySelectorAll('.cat-filter-btn').forEach(b => {
        b.className = 'cat-filter-btn px-3 py-1.5 rounded text-xs font-oswald uppercase tracking-wider bg-[#141414] hover:bg-[#222222] text-neutral-400 transition-all';
      });
      btn.className = 'cat-filter-btn px-3 py-1.5 rounded text-xs font-oswald uppercase tracking-wider bg-white text-black font-bold transition-all';
      renderModalCoaches();
    });
  });
}

document.addEventListener('DOMContentLoaded', () => {
  initCoachesCarousel();
  initCoachesModalEvents();
});
